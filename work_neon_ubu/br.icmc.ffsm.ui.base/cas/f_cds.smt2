(define-sort Feature () Bool)
(declare-const A Feature)
(declare-const B Feature)
(declare-const U Feature)
(declare-const Y Feature)
(declare-const C Feature)
(declare-const T Feature)
(declare-const L Feature)
(declare-const N Feature)
(declare-const W Feature)

(assert A)
(assert (and
   (= B A)
   (=> U B)
   (= Y B)
   (= (or C  T ) Y)
   (not (and C T))
   (= L A)
   (= N L)
   (= W L)
))

(push)
(assert (and (or C T) true U))
(assert (and 
    (not (and true true true C (or C T) true ))
    (not (and true true true T (or C T) true ))
))
(check-sat)
(pop)
(push)
(assert (and (or C T) true true))
(assert (and 
    (not (and true true true C (or C T) true ))
    (not (and true true true T (or C T) true ))
))
(check-sat)
(pop)
(push)
(assert (and true true true))
(assert (and 
    (not (and true true ))
))
(check-sat)
(pop)
(push)
(assert (and true true true))
(assert (and 
    (not (and true true ))
))
(check-sat)
(pop)
(push)
(assert (and U true true))
(assert (and 
    (not (and true true true C (or C T) true U true ))
    (not (and true true true T (or C T) true U true ))
))
(check-sat)
(pop)
(push)
(assert (and true true true))
(assert (and 
    (not (and true true ))
))
(check-sat)
(pop)
(push)
(assert (and true true true))
(assert (and 
    (not (and true true true true ))
))
(check-sat)
(pop)
(push)
(assert (and U true true))
(assert (and 
    (not (and true true true C (or C T) true U true ))
    (not (and true true true T (or C T) true U true ))
))
(check-sat)
(pop)
(push)
(assert (and true C (or C T)))
(assert (and 
    (not (and true true true C ))
))
(check-sat)
(pop)
(push)
(assert (and true T (or C T)))
(assert (and 
    (not (and true true true T ))
))
(check-sat)
(pop)
(push)
(assert (and (or C T) (not U) true))
(assert (and 
    (not (and true true true C (or C T) (not U) ))
    (not (and true true true T (or C T) (not U) ))
))
(check-sat)
(pop)
