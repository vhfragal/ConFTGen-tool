(define-sort Feature () Bool)
(declare-const G Feature)
(declare-const A Feature)
(declare-const M Feature)
(declare-const L Feature)
(declare-const C Feature)
(declare-const R Feature)
(declare-const B Feature)
(declare-const N Feature)
(declare-const W Feature)
(declare-const V Feature)
(declare-const Y Feature)
(declare-const P Feature)
(declare-const S Feature)

(assert G)
(assert (and
   (= A G)
   (= M A)
   (= L A)
   (= C G)
   (= R G)
   (= (or B  N  W ) R)
   (not (and B N))
   (not (and B W))
   (not (and N W))
   (= V G)
   (= Y V)
   (= P V)
   (=> S V)
))

(push)
(assert true)
(assert (and 
    (not (and true N))
    (not (and true W))
    (not (and true B))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and (not W) true))
    (not (and W true))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and (or (not W) S) true))
    (not (and (and W (not S)) true))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and S true))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and true N))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and S true))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and true W))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and (not S) W))
    (not (and S true))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and S S))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and true B))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and true B))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and S true))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and true N))
    (not (and true W))
    (not (and true B))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and true true))
))
(check-sat)
(pop)
(push)
(assert true)
(assert (and 
    (not (and B S))
    (not (and N S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and true N))
    (not (and true W))
    (not (and true B))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and (not W) S))
    (not (and W S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and true S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and true S))
))
(check-sat)
(pop)
