(define-sort Feature () Bool)
(declare-const G Feature)
(declare-const A Feature)
(declare-const M Feature)
(declare-const L Feature)
(declare-const C Feature)
(declare-const R Feature)
(declare-const B Feature)
(declare-const N Feature)
(declare-const W Feature)
(declare-const V Feature)
(declare-const Y Feature)
(declare-const P Feature)
(declare-const S Feature)

(assert G)
(assert (and
   (= A G)
   (= M A)
   (= L A)
   (= C G)
   (= R G)
   (= (or B  N  W ) R)
   (not (and B N))
   (not (and B W))
   (not (and N W))
   (= V G)
   (= Y V)
   (= P V)
   (=> S V)
))

(push)
(assert (and 
    (and true (not W) true)
    (and true W true)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true (or (not W) S) true)
    (and true (and W (not S)) true)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true N)
    (and true true W)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true N)
    (and true true B)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true W)
    (and true true B)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and W (not S) W)
    (and W S true)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true N)
    (and true true W)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true N)
    (and true true B)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true true W)
    (and true true B)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and true B S)
    (and true N S)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and S (not W) S)
    (and S W S)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and S true N)
    (and S true W)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and S true N)
    (and S true B)
))
(check-sat)
(pop)
(push)
(assert (and 
    (and S true W)
    (and S true B)
))
(check-sat)
(pop)
